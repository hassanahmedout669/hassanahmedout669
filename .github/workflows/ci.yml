name: Super-Lint

on: push

jobs:
  test_schedule:
    runs-on: [self-hosted, runner-1]
    steps:
      - name: Not on Monday or Wednesday
        run: touch runner1file
      #  run: git pull && bash runFile.sh 
